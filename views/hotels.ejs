<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Hotel List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/user/dashboard">Hotel Booking System</a>
        <a href="/profile" class="btn btn-light ms-auto">Profile</a>
    </div>
</nav>

<div class="container mt-4">
    <h2>Available Hotels</h2>

    <div class="row">
        <% hotels.forEach(hotel => { %>
        <div class="col-md-4">
            <div class="card mb-4 shadow">
                <img src="/uploads/<%= hotel.filename %>" class="card-img-top" alt="Hotel Image">
                <div class="card-body">
                    <h5 class="card-title"><%= hotel.hotel_name %></h5>
                    <p class="card-text">
                        <strong>City:</strong> <%= hotel.city_name %><br>
                        <strong>Area:</strong> <%= hotel.area_name %><br>
                        <strong>Rating:</strong> <%= hotel.rating %> ⭐
                    </p>
                    <a href="/bookHotel/<%= hotel.hotel_id %>" class="btn btn-primary w-100">Book Now</a>
                </div>
            </div>
        </div>
        <% }); %>
    </div>
</div>

</body>
</html> -->
